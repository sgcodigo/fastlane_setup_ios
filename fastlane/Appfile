for_platform :ios do
  for_lane :staging do
    app_identifier ENV['STAGING_APP_IDENTIFIER']
    team_id ENV['STAGING_TEAM_ID']
    apple_id ENV['STAGING_APPLE_ID']
    team_name ENV['STAGING_TEAM_NAME']
  end

  for_lane :uat do
    app_identifier ENV['UAT_APP_IDENTIFIER']
    team_id ENV['UAT_TEAM_ID']
    apple_id ENV['UAT_APPLE_ID']
    team_name ENV['UAT_TEAM_NAME']
  end
end